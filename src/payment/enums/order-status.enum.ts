export enum OrderStatus {
  INITIATED = 'initiated',
  PENDING = 'pending',
  PROCESSING = 'processing',
  CANCELLED = 'cancelled',
  FULFILLED = 'fulfilled',
  VALIDATED = 'validated',
  SETTLED = 'settled',
  EXPIRED = 'expired',
}